{foreach $event->rounds as $round_num => $round}{if $round_num == $round_number}{$flight_type_id = $round.flight_type_id}{$subs = $event->flight_types.$flight_type_id.flight_type_sub_flights}{/if}{/foreach}"Pilot_id"{$fs}"First_Name"{$fs}"Last_Name"{$fs}"group"{$fs}"total_minutes"{$fs}"total_seconds"{$fs}"penalty"{for $x = 1 to $subs}{$fs}"sub{$x}"{/for}
{foreach $rounds as $epid => $round}{foreach $round as $round_num => $r}{if $round_number == $round_num}"{$r.pilot_id}"{$fs}"{$r.pilot_first_name}"{$fs}"{$r.pilot_last_name}"{$fs}"{$r.event_pilot_round_flight_group}"{$fs}"{$r.event_pilot_round_flight_minutes}"{$fs}"{$r.event_pilot_round_flight_seconds}"{$fs}"{$r.event_pilot_round_flight_penalty}"{foreach $r.sub as $s}{$fs}"{$s.event_pilot_round_flight_sub_val}"{/foreach}{/if}{/foreach}
{/foreach}